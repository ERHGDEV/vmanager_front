const Header = () => {
    return (
        <header>
        <h1 className="text-2xl text-gray-200" >V Manager</h1>
        </header>
    )
}

// verificar el button de log out cuando se tenga implementado el login

export default Header